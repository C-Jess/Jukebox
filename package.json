{
  "name": "jukebox",
  "version": "1.0.0",
  "description": "",
  "main": "server.js",
  "scripts": {
    "devstart": "concurrently \"npm run server\" \"npm start --prefix client\"",
    "start": "node server/server.js",
    "heroku-postbuild": "cd client && npm install && npm run build",
    "server": "nodemon server/server.js"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "concurrently": "^5.2.0",
    "cors": "^2.8.5",
    "dotenv": "^10.0.0",
    "express": "^4.17.1",
    "path": "^0.12.7",
    "querystring": "^0.2.0",
    "request": "^2.88.2"
  },
  "devDependencies": {
    "nodemon": "^2.0.4"
  }
}
